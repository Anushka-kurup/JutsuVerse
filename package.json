{
  "name": "jutsuverse",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w @jutsu/protocol && npm run typecheck -w server && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
