{
  "name": "jutsuverse",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,frontend -c blue,green \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "test": "npm test -w server",
    "typecheck": "npm run typecheck -w @jutsu/protocol && npm run typecheck -w server && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
