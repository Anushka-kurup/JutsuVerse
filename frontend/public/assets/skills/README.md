# public/assets/skills/

Currently unused — the skill-cast banner shows the jutsu **name only** (see
`src/ui/sealVisual.ts` `skillHtml`). If you want per-skill art back, re-add the
`<img>` there and drop `<id>.png` files here (ids from `shared/skills.ts`:
clone, fireball, water_trumpet, great_breakthrough, earth_dragon).
