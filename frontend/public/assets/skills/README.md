# public/assets/skills/

One image per skill, `<id>.png` (ids from `shared/skills.ts`):

    clone  fireball  water_trumpet  great_breakthrough  substitution

Shown as a big centre banner for 3 seconds when that jutsu is cast
(`Overlay.flashSkill`), and — small — in the skill dock. Missing files fall back
to the JP + EN name text.
